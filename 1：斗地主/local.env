# 配置模板，不含真实密钥。复制为 .env 后填写；已有 .env 不要覆盖。
# 当前 Agent 接收 config 字典，环境读取与类型转换由调用方负责。
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=

# 对应 Agent.config 的 timeout、retries、backoff。
DDZ_API_TIMEOUT=120
DDZ_API_RETRIES=2
DDZ_API_BACKOFF=1

# 对应 max_tokens；留空表示不传该参数，上限由服务决定。
DDZ_MAX_TOKENS=
